# Optional: a default Anthropic API key used as a fallback.
# In normal operation users supply their key in the UI (per-session).
# This file is only read at backend startup if no key is supplied via header.
ANTHROPIC_API_KEY=
